#include <stdio.h>

int main(){
    int count,n,arr[n];
    scanf("%d", &n);
    for(int k=0; k<n; k++){
        scanf("%d", &arr[k]);
    }
    for(int i=0;i<n;i++){
        printf("%d ",count+=arr[i]);
    }
}